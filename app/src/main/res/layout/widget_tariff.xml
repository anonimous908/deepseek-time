<?xml version="1.0" encoding="utf-8"?>
<!--
  widget_tariff.xml — layout del widget (4x2).

  Solo se usan vistas soportadas por RemoteViews: LinearLayout, TextView,
  Chronometer e ImageView. NO se puede usar Compose aquí.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="12dp"
    android:background="@drawable/widget_bg_offpeak">

    <!-- Fila 1: Encabezado con estado y badge de tarifa -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/tv_tariff_label"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:maxLines="1"
            android:ellipsize="end"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:text="@string/tariff_peak" />

        <TextView
            android:id="@+id/tv_tariff_badge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:paddingTop="2dp"
            android:paddingBottom="2dp"
            android:textSize="10sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:background="@drawable/widget_badge_peak"
            android:text="@string/tariff_peak_short" />
    </LinearLayout>

    <!-- Fila 2: Cuenta regresiva -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="3dp">

        <TextView
            android:id="@+id/tv_countdown_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="11sp"
            android:textColor="#888888"
            android:text="@string/countdown_to_offpeak"
            android:layout_marginEnd="6dp" />

        <Chronometer
            android:id="@+id/ch_countdown"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF" />
    </LinearLayout>

    <!-- Precios vigentes: una línea por modelo -->
    <TextView
        android:id="@+id/tv_price_1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="3dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:textSize="11sp"
        android:textColor="#B0B0B0"
        android:visibility="gone" />

    <TextView
        android:id="@+id/tv_price_2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="1dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:textSize="11sp"
        android:textColor="#B0B0B0"
        android:visibility="gone" />

    <!-- Aviso de estado degradado (catálogo no disponible) -->
    <TextView
        android:id="@+id/tv_catalog_note"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="3dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:textSize="11sp"
        android:textColor="#FFA726"
        android:text="@string/degraded_title"
        android:visibility="gone" />

    <!-- Mini-timeline del día (Bitmap dibujado con Canvas) -->
    <ImageView
        android:id="@+id/iv_timeline"
        android:layout_width="match_parent"
        android:layout_height="12dp"
        android:layout_marginTop="6dp"
        android:scaleType="fitXY"
        android:contentDescription="@string/timeline_title" />

</LinearLayout>
